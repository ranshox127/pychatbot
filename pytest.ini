[pytest]
testpaths = tests
addopts = -q
markers =
    unit: pure logic, no external IO
    infrastructure: real DBs IO
    contract: adapter/gateway contract with mocks, no real IO
    integration: touches real databases
    e2e: cross-layer/system scenarios (may hit DB, usually through HTTP)
